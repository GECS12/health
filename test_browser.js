const { chromium } = require('playwright'); (async () => { try { const browser = await chromium.launch(); console.log('Browser launched successfully'); await browser.close(); } catch (e) { console.error('Launch failed:', e.message); process.exit(1); } })();
